# Penpot MCP Server configuration
# Generate your token at: Penpot → Settings → Access Tokens
PENPOT_ACCESS_TOKEN=your-penpot-access-token-here
PENPOT_API_URL=https://design.penpot.app

# HTTP server settings
TRANSPORT=http
HTTP_PORT=3100
HTTP_HOST=0.0.0.0
